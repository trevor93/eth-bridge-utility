// Utility functions

function formatBalance(balance) {
    // Format balance to human readable
}

function log(message) {
    console.log(`[${new Date().toISOString()}] ${message}`);
}

module.exports = { formatBalance, log };
